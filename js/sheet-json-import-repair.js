/* Legacy compatibility shim.
 * JSON control-character repair and imported style-skill name reconciliation
 * are now owned by sheet-import-style-skill-compat.js.
 * Keep this file temporarily because sheet.html still references it.
 */
