# CLAUDE.md

このファイルはClaude Codeがセッション開始時に自動で読み込む。
詳細な運用ルールは docs/AI_HANDOFF.md が正本なので、作業前に必ずそちらも確認すること。
このファイルは「毎回説明し直すコスト」を削るための要点だけをまとめたもの。

## プロジェクト概要

『トーキョーN◎VA THE AXLERATION』の非公式ファンツール、キャラクターシート管理サイト
「CAST ARCHIVE」。純粋な静的サイト(ビルドツールなし、ES Modules + Supabase)。

## リポジトリの構成

- このrepo(tnx-cast-archive-test) = **検証環境**。実装・テストはここで行う。
- inarin14311431/tnx_cast_list = **本番環境**。検証で確認が取れたものだけを同期する。
- 検証・本番は**同一のSupabaseプロジェクト**(koprmbkoftuuffslhsvt)を共有している。
  DBへのDDL変更(migration実行)は、repoへのcommitと実DBへの適用を別ゲートとして扱う。
  実DB適用は必ず明示的な確認を取ってから行う。
- 検証・本番でruntime(js/css-next/*.html/config/supabase)は同一に保つ運用だが、
  テスト構成(tests/配下、CI設定)は意図的に異なる。テストファイルは同期しない。

## 必須コマンド

- `npm run verify` — 全19 audit + 全テストを実行する最終確認コマンド。
  **通常は作業の最後に1回だけ**実行する。
- 調査・実装の途中では `node --test tests/該当ファイル.test.mjs` のように
  関連ファイルだけを絞って実行し、無駄なテスト出力でコンテキストを圧迫しない。
- `npm run audit:*` は個別監査(循環依存・移行整合性など)。必要な時だけ実行する。

## 変更の進め方

1. mainから作業branchを切る(1つの目的に絞る)
2. 関連テストを追加・更新する
3. `npm run verify` で確認する
4. commit・push・PR作成
5. 目視確認が必要な変更(見た目・タイミング絡み)は、実機/Playwrightで
   実際に確認してから報告する。「直したはず」で終わらせない。

## テストについて絶対に守ること

- 失敗したテストは、まず実装とテストのどちらが誤っているかを調査する。
  テストを弱めて変更を通さない。
- 過去のPRの記録(コミットメッセージ)と実際の挙動が食い違う場合は、
  その場で確認し、正直に報告する。憶測で「たぶん大丈夫」と言わない。
- 安全に直せないと分かった場合は、無理に押し通さず変更を元に戻し、
  分かったことを正直に報告する(実際に何度かこのパターンで助かっている)。

## トークン・コストを抑えるための習慣

- 定型的な1〜2ファイルの修正はSonnetで十分。広範囲の調査・設計判断が
  必要な場面でだけOpusに切り替える。
- 話題が大きく変わるタイミング(例: 個別バグ修正→本番同期)では `/clear` を検討する。
- 「repoを見回して」のような曖昧な依頼を避け、対象ファイル・原因の仮説を
  先に絞ってから作業を始める(実際に効果があった進め方)。

## 日本語で回答すること

このプロジェクトでのやり取りは日本語で行う。
