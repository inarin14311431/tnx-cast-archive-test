# Developer Guide

トーキョーN◎VA CAST ARCHIVE の開発・保守を行うためのガイド。

このアプリでは、機能追加やデザイン変更の速度よりも、既存のキャストデータ・表示・編集機能を壊さないことを優先する。

## 1. 開発の基本原則

1. 既存機能を後付けfixで上書きせず、担当モジュールを特定して修正する。
2. 同じDOMやデータを複数モジュールが扱う場合は、イベント所有権と責務を明確にする。
3. CSSは画面固有の数値を無秩序に追加せず、共通コンポーネント・レイアウト・テーマのどこで管理すべきか判断する。
4. データ構造を変更する場合は、保存・読込・旧データ互換・閲覧表示まで一連で確認する。
5. 機能追加・仕様変更・デザイン変更を完了する前に、Node回帰・E2E・Visual Regressionの安全網を通す。
6. テスト時間を短縮するために安全網を削らない。同一原因・同一層の無駄な重複だけ整理する。

## 2. 主な構成

### HTML

各画面のエントリポイント。PC編集、モバイル編集、閲覧、アカウント、バックアップ等の画面を持つ。

HTMLへ新しいscriptやstylesheetを追加する前に、既存モジュールで担当できないか確認する。

### `js/`

画面動作、データ変換、保存・読込、描画、操作制御を担当する。

新規ファイルを作る場合は「特定不具合を直すfixファイル」ではなく、機能上の責務が名前から分かる単位にする。

避ける例:

```text
xxx-fix.js
xxx-final-fix.js
xxx-fixes.js
```

推奨:

```text
cast-view-controls.js
sheet-birthplace.js
outfit-xxx.js
```

既存の歴史的なfix系ファイルは、現役ロジックを含む場合があるため名前だけで削除しない。

### `css-next/`

共通CSSの主要構造。Foundation / Layout / Componentsなどの責務に分けて管理する。

デザイン変更時は、特定画面だけに効かせるべきか、共通UIとして全画面へ効かせるべきかを先に判断する。

### テーマ

テーマ固有の色・装飾と、共通UIの形状・配置を混在させない。

テーマを追加・削除する場合は、テーママニフェスト、トークン、コントラスト、Visual Regressionまで確認する。

### `tests/`

Node契約・回帰テストとPlaywright E2Eを格納する。

テスト戦略の詳細は `TESTING.md` を参照する。

### `scripts/`

静的監査、runtime監査、テストランナー、DOM ownershipレポート等を格納する。

### `supabase/`

DB関連の定義・migrationを管理する。適用済みmigrationも履歴なので原則削除しない。

## 3. セットアップ

Node.js / npm が利用できる環境で依存関係を取得する。

```bash
npm ci
```

Playwrightをローカル実行する場合は必要なブラウザを取得する。

```bash
npm run e2e:install
```

Playwright本体のバージョンを変更した場合は、GitHub Actionsで使用しているPlaywright Dockerイメージも同じバージョンへ合わせる。バージョン不一致は契約テストでも検出する。

## 4. 通常の開発フロー

### 実装途中

変更対象に応じて軽量確認を使う。

```bash
npm run test:core
npm run test:presentation
```

`test:core` は保存、読込、計算、データマッピング、状態遷移など。

`test:presentation` はCSS、hover/focus、レスポンシブ、重要なレイアウト契約など。

これらは開発途中用であり、変更完了時の最終確認にはしない。

### 変更完了

```bash
npm test
npm run verify
```

`npm run verify` は以下をまとめて確認する。

- JavaScript構文
- モジュール依存
- runtime integrity
- CSS構造
- テーマ
- sheet runtime
- cast runtime
- troop runtime
- mobile runtime
- security
- sheet/cast DOM ownership
- 全Node回帰テスト

### Pull Request

PRでは次の3系統をすべて成功させる。

```text
Regression checks
Playwright E2E
Visual Regression
```

CIが赤い状態を「テスト側の問題だろう」と判断してそのままマージしない。テストが古い契約を要求している場合も、原因を特定して契約そのものを更新する。

## 5. 機能追加の手順

1. 既存の担当モジュールを特定する。
2. 保存データへ影響する場合は、入力→内部データ→Supabase→再読込→閲覧表示の経路を確認する。
3. 既存データとの互換性が必要か判断する。
4. 最小限の実装を行う。
5. 同種の既存Nodeテストへケースを追加する。
6. 複数画面をまたぐ操作ならE2Eを追加・更新する。
7. 見た目が変わる場合はVisual Regressionを確認する。
8. `npm run verify` とPR CIを通す。

特に能力値・制御値・CS・技能・アウトフィット・経験点などは互いに影響するため、入力画面だけ正常でも完了とはみなさない。

## 6. デザイン変更の手順

1. 変更が共通UI、特定画面、テーマ固有のどこに属するか決める。
2. 既存CSSの担当箇所を修正する。
3. 同じ要素を別CSSから上書きしていないか確認する。
4. PCとmobileを確認する。
5. 全テーマへの影響を確認する。
6. Presentation契約を確認する。
7. Visual Regressionの差分を確認する。

基準画像の更新は、差分が意図した変更であることを確認した場合だけ行う。

```bash
npm run visual
npm run visual:update
```

`visual:update` を「テストを通すため」に使用しない。

## 7. テーマ追加・変更

テーマは色だけでなく、ボタン、枠、背景、入力欄、テーブル、状態表示などアプリ全体へ影響する。

追加時は最低限以下を確認する。

- テーマ選択肢へ登録されている
- 必要なトークンが揃っている
- 共通コンポーネントへテーマが適用される
- hover / focus / disabled等で読めなくならない
- PC / mobile双方で破綻しない
- 既存テーマへ副作用がない
- `npm run audit:themes` が成功する
- Visual Regressionが成功する

テーマ固有CSSでレイアウトそのものを変更するのは原則避ける。レイアウトは共通側、色・光・影・質感はテーマ側という分担を基本とする。

## 8. データ項目・アウトフィット項目の追加

新しい項目を追加する場合は、画面にinputを増やすだけでは不十分。

確認対象:

```text
編集UI
↓
DOMからの取得
↓
内部データ構造
↓
保存マッピング
↓
Supabase
↓
読込・正規化
↓
編集画面への復元
↓
閲覧画面へのマッピング
↓
必要なら経験点・集計等へ反映
```

旧データで値が存在しない場合の初期値も定義する。

過去に編集画面では入力できるが閲覧画面へ反映されない種類の不具合があったため、往復経路を必ずテストする。

## 9. DOM ownership

複数モジュールが同じDOMを参照すること自体は禁止しない。

問題になるのは、複数モジュールが同じイベントや描画責務を所有し、どちらが最終状態を決めるか不明になる状態。

確認には以下を使用する。

```bash
npm run report:sheet-ownership
npm run report:cast-ownership
```

新しいイベントリスナーやMutationObserverを追加する前に、既に同じDOMを監視しているモジュールがないか確認する。

可能な場合は、DOM変化をMutationObserverで後追いするより、描画側が完了イベントを発行し、利用側が購読する構造を優先する。

## 10. テスト追加の判断

不具合修正では再発防止テストを追加する。

ただし、同じ原因を同じ方法で検査するテストを増殖させない。

### Coreに置くもの

- 保存・読込
- データマッピング
- 計算
- 正規化
- 互換性
- 状態遷移
- モジュール責務

### Presentationに置くもの

- hover / focus
- タッチ領域
- レスポンシブ構造
- 意味のある列幅等のUI契約

### E2Eに置くもの

- 実ブラウザ操作
- 保存→再読込
- 画面遷移
- mobile操作
- 複数機能の連携

### Visual Regressionに置くもの

- 配置ずれ
- 色
- 枠
- テーマ
- 見切れ
- 最終的な描画結果

NodeとE2EとVisualが同じ機能を守ることは、検出層が異なるため必ずしも重複ではない。

## 11. 不要ファイルの判断

ファイル名だけで削除しない。

特に `backup`、`legacy`、`fix`、`old` のような名称でも現役機能や互換処理を持つ場合がある。

削除前に確認する。

1. HTMLから参照されていない
2. JS/CSSからimport・動的ロードされていない
3. CI / scripts / testsから参照されていない
4. 互換処理として必要ではない
5. DB migration履歴ではない
6. Visual Regressionの基準画像ではない
7. 開発・運用ドキュメントとして役割がない

「アプリ実行時に読まれない」と「リポジトリに不要」は同義ではない。

## 12. Supabase・セキュリティ

データアクセスを変更する場合はRLSを前提にする。

特にキャスト本体だけでなく、skills / outfits / combos等の関連データも親キャストの所有権・公開状態との整合を保つ。

変更後は以下を実行する。

```bash
npm run audit:security
npm run verify
```

認証情報、`.env`、ローカル生成物をコミットしない。

## 13. 互換コードの扱い

旧キャッシュや旧データを救済するshimを追加する場合は、恒久機能として扱わない。

- 何を救済するコードかコメントする
- 新規実装がそのshimへ依存しないようにする
- 十分な移行期間後に削除候補として監査する

互換コードの上にさらに互換コードを重ねない。

## 14. リファクタリングの基準

ファイル数を減らすこと自体を目的にしない。

実施価値が高い状態:

- 同じイベントを複数モジュールが所有している
- 同じデータ変換が複数箇所にある
- CSSの同じ責務を複数ファイルが上書きしている
- MutationObserverによる後処理が増殖している
- 同じ不具合を同一層の複数テストが重複検査している

無理に触らなくてよい状態:

- 複数モジュールが同じDOMを読むだけで責務は異なる
- 歴史的な名前だが現役ロジックを持つ
- 異なる検出層のテストが同じ機能を守っている

## 15. 完了条件

機能追加・修正は、コードを書き終えた時点では完了ではない。

最低限、以下を満たした状態を完了とする。

```text
[ ] 対象機能が期待通り動作する
[ ] 保存・再読込後もデータが維持される
[ ] 閲覧画面へ正しく反映される
[ ] PC / mobileへの副作用を確認した
[ ] 既存テーマへの副作用を確認した
[ ] npm test が成功する
[ ] npm run verify が成功する
[ ] Playwright E2E が成功する
[ ] Visual Regressionの差分を確認した
[ ] 不要なfix・一時ファイルを残していない
```

詳細なテスト運用は `TESTING.md` を正本とする。
